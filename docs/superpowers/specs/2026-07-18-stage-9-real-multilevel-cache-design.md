# 阶段九真实多级缓存设计

## 状态

本文原先同时设计多级缓存与独立缓存故障实验。项目随后决定取消独立缓存故障实验，
缓存穿透、击穿、雪崩和保护策略只保留理论课程内容。

当前阶段九的完整、有效设计以
[阶段九多级缓存层级控制与故障理论收敛设计](./2026-07-18-stage-9-cache-layer-controls-design.md)
为准。

## 保留范围

阶段九保留以下真实运行能力：

* 三台固定等权应用实例。
* 每台实例独立的进程内 L1。
* 一个实验专属 Redis L2。
* 实验隔离 MySQL 回源。
* 每个 HTTP 批次执行一次真实缓存查询。
* 十二个商品、每实例九项 LRU、L1 和 Redis TTL。
* 后端真实命中轨迹和模拟耗时。
* 三台 L1 与共享 Redis 的库存、版本和 TTL 展示。
* 指定实例 L1 的添加和去除。
* 共享 Redis 容器的真实删除和重新创建。

## 取消范围

阶段九不再提供：

* 独立 `cache_failures` 场景模板或实验入口。
* 缓存穿透、击穿或雪崩交互实验。
* 空值缓存、布隆过滤器、`singleflight`、Redis 锁、提前续期、回源限流或降级策略开关。
* 任意 TTL、容量、Redis key、Redis 命令或 Docker 参数输入。

“缓存故障”课程继续保留为纯理论课程，且 `labAvailable=false`。
