openapi: 3.1.0
info:
  title: Backend Architecture Learning Platform API
  version: 0.3.0
  license:
    name: MIT
    identifier: MIT
servers:
  - url: /
security: []
tags:
  - name: System
  - name: Authentication
  - name: Courses
  - name: Labs
paths:
  /readyz:
    get:
      tags: [System]
      summary: Check platform readiness
      operationId: getReadiness
      responses:
        "200":
          description: Process and platform database are ready.
        "503":
          description: A required dependency is unavailable.
  /api/v1/system/info:
    get:
      tags: [System]
      summary: Get platform build information
      operationId: getSystemInfo
      responses:
        "200":
          description: Platform build information.
  /api/v1/auth/login:
    post:
      tags: [Authentication]
      summary: Log in with a pre-created test account
      operationId: login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Login succeeded and a Session Cookie was set.
          headers:
            Set-Cookie:
              description: HttpOnly Session Cookie with SameSite=Strict.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthenticationResponse"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          description: Username or password is invalid.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: The account is disabled.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Too many failed login attempts.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/auth/logout:
    post:
      tags: [Authentication]
      summary: Revoke the current Session
      operationId: logout
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFToken"
      responses:
        "200":
          description: Session was revoked and the Session Cookie was cleared.
          headers:
            Set-Cookie:
              description: Expired Session Cookie.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogoutResponse"
        "401":
          $ref: "#/components/responses/AuthRequired"
        "403":
          $ref: "#/components/responses/CSRFInvalid"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/auth/me:
    get:
      tags: [Authentication]
      summary: Get the current authenticated user
      operationId: getCurrentUser
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Current user and Session metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthenticationResponse"
        "401":
          $ref: "#/components/responses/AuthRequired"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/courses:
    get:
      tags: [Courses]
      summary: List knowledge-map courses
      operationId: listCourses
      security:
        - sessionCookie: []
        - {}
      responses:
        "200":
          description: Knowledge-map courses in display order with optional user progress.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseListResponse"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/courses/{slug}:
    get:
      tags: [Courses]
      summary: Get one course and record an authenticated user's view
      operationId: getCourse
      security:
        - sessionCookie: []
        - {}
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
            pattern: "^[a-z0-9][a-z0-9-]{0,127}$"
      responses:
        "200":
          description: Course metadata, theory content, lab availability, and optional progress.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseDetailResponse"
        "404":
          description: Course does not exist.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/labs:
    post:
      tags: [Labs]
      summary: Create a lab session
      operationId: createLab
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFToken"
      responses:
        "401":
          $ref: "#/components/responses/AuthRequired"
        "403":
          $ref: "#/components/responses/CSRFInvalid"
        "501":
          description: Reserved for the lab implementation phase.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/labs/{labId}/traffic-batches:
    post:
      tags: [Labs]
      summary: Submit one frontend-generated traffic batch
      operationId: submitTrafficBatch
      security:
        - sessionCookie: []
      parameters:
        - name: labId
          in: path
          required: true
          schema:
            type: string
            pattern: "^[a-z0-9][a-z0-9-]{3,63}$"
        - $ref: "#/components/parameters/CSRFToken"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TrafficBatchRequest"
      responses:
        "200":
          description: Batch was processed, partially processed, or dropped by capacity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrafficBatchResponse"
        "400":
          description: Batch fields, product, or request units are invalid.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/AuthRequired"
        "403":
          $ref: "#/components/responses/CSRFInvalid"
        "404":
          description: The lab does not exist or is not owned by the user.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The lab is not running.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: The platform HTTP rate limit was exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: The lab gateway, instance, or internal response is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: session
      description: HttpOnly Session Cookie set by the login endpoint.
  parameters:
    CSRFToken:
      name: X-CSRF-Token
      in: header
      required: true
      description: CSRF token returned by login or the current-user endpoint.
      schema:
        type: string
        minLength: 1
  responses:
    ValidationFailed:
      description: Request JSON or fields are invalid.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    AuthRequired:
      description: A valid Session Cookie is required.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    CSRFInvalid:
      description: The CSRF token is missing or invalid.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    InternalError:
      description: The request could not be completed.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
  schemas:
    LoginRequest:
      type: object
      additionalProperties: false
      required: [username, password]
      properties:
        username:
          type: string
          minLength: 1
          maxLength: 64
          example: learner
        password:
          type: string
          format: password
          minLength: 1
          maxLength: 72
          example: example-password
    User:
      type: object
      required: [id, username, status]
      properties:
        id:
          type: integer
          format: int64
        username:
          type: string
        status:
          type: string
          enum: [active, disabled]
    AuthenticationResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [user, csrfToken, sessionExpiresAt]
          properties:
            user:
              $ref: "#/components/schemas/User"
            csrfToken:
              type: string
            sessionExpiresAt:
              type: string
              format: date-time
    LogoutResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [loggedOut]
          properties:
            loggedOut:
              type: boolean
              const: true
    CourseProgress:
      type: object
      required: [viewed, lastViewedAt, lastLabId]
      properties:
        viewed:
          type: boolean
        lastViewedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        lastLabId:
          oneOf:
            - type: string
            - type: "null"
    Course:
      type: object
      required:
        - id
        - slug
        - title
        - category
        - status
        - sortOrder
        - summary
        - labAvailable
        - progress
      properties:
        id:
          type: integer
          format: int64
        slug:
          type: string
        title:
          type: string
        category:
          type: string
        status:
          type: string
          enum: [theory, active, coming_soon]
        sortOrder:
          type: integer
        summary:
          type: string
        labAvailable:
          type: boolean
        progress:
          oneOf:
            - $ref: "#/components/schemas/CourseProgress"
            - type: "null"
    CourseDetail:
      allOf:
        - $ref: "#/components/schemas/Course"
        - type: object
          required:
            - content
            - contentFormat
            - implementation
            - lab
          properties:
            content:
              type: string
            contentFormat:
              type: string
              const: markdown
            implementation:
              type: object
              required: [requestPath, keyConcepts]
              properties:
                requestPath:
                  type: array
                  items:
                    type: string
                keyConcepts:
                  type: array
                  items:
                    type: string
            lab:
              type: object
              required: [available]
              properties:
                available:
                  type: boolean
                scenarioType:
                  type: string
                allowedInstanceRange:
                  type: object
                  required: [min, max]
                  properties:
                    min:
                      type: integer
                    max:
                      type: integer
    CourseListResponse:
      type: object
      required: [data, meta]
      properties:
        data:
          type: object
          required: [courses]
          properties:
            courses:
              type: array
              items:
                $ref: "#/components/schemas/Course"
        meta:
          type: object
          required: [authenticated, total]
          properties:
            authenticated:
              type: boolean
            total:
              type: integer
    CourseDetailResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [course]
          properties:
            course:
              $ref: "#/components/schemas/CourseDetail"
    TrafficBatchRequest:
      type: object
      additionalProperties: false
      required: [batchId, productId, requestUnits]
      properties:
        batchId:
          type: string
          minLength: 1
          maxLength: 64
        productId:
          type: integer
          format: int64
          minimum: 1
        requestUnits:
          type: integer
          minimum: 1
          maximum: 100
    TrafficBatchResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [result]
          properties:
            result:
              $ref: "#/components/schemas/TrafficBatchResult"
    TrafficBatchResult:
      type: object
      required:
        - batchId
        - labId
        - status
        - occurredAt
        - path
        - targetInstanceId
        - receivedUnits
        - processedUnits
        - droppedUnits
        - instanceState
      properties:
        batchId:
          type: string
        labId:
          type: string
        status:
          type: string
          enum: [processed, partially_processed, dropped]
        occurredAt:
          type: string
          format: date-time
        path:
          type: array
          minItems: 2
          items:
            type: string
        targetInstanceId:
          type: string
        receivedUnits:
          type: integer
          minimum: 0
        processedUnits:
          type: integer
          minimum: 0
        droppedUnits:
          type: integer
          minimum: 0
        instanceState:
          $ref: "#/components/schemas/TrafficInstanceState"
    TrafficInstanceState:
      type: object
      required: [effectiveCapacity, remainingCapacity, loadRatio, loadState]
      properties:
        effectiveCapacity:
          type: integer
          minimum: 0
        remainingCapacity:
          type: integer
          minimum: 0
        loadRatio:
          type: number
          minimum: 0
        loadState:
          type: string
          enum: [idle, normal, high, overloaded, unavailable]
    ErrorResponse:
      type: object
      required: [error, requestId]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
            message:
              type: string
        requestId:
          type: string
