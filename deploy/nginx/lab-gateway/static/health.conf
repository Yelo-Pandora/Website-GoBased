server {
  listen 8080 default_server;
  server_name _;

  location = /healthz {
    access_log off;
    default_type application/json;
    return 200 '{"service":"lab-gateway-nginx","status":"ok"}\n';
  }

  location / {
    return 404;
  }
}
